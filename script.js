const fs = require('fs');

const content = 'This was generated by a GitHub action. (script.js)';

fs.writeFile('test.txt', content, err => {
    if (err) {
        console.error(err);
    }
    // file written successfully
});